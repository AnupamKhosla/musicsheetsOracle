{
  "apps": [{
    "name": "musicsheets",
    "script": "server.js",
    "instances": 1,
    "exec_mode": "fork",
    "max_memory_restart": "300M",
    "env": {
      "PORT": "5050",
      "NODE_ENV": "production"
    }
  }]
}
